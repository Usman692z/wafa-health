// All data operations have been migrated to Supabase (PostgreSQL).
// This file re-exports everything from supabase.ts for backwards compatibility
// so existing imports across the codebase continue to work without changes.
export * from './supabase';
